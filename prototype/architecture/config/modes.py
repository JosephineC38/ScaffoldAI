# "Step-by-step" and "Check-my-plan" are deferred modes to be added later —
# keep this as a flat list of strings so extending it stays a one-line change.
MODES = ["Tutor", "Concept Explanation", "Hint-only"]
